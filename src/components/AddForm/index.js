import styles from './AddForm.module.scss';
import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

function AddForm({ datatitle, data }) {
    return (<h1>Add Form</h1>);
}

export default AddForm;